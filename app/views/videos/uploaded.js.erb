<% if @video.errors.any? %>
  $.alert("<%= I18n.t(:upload_error).html_safe %>", "error");
  $(top.document).trigger("hide-popovers");
<% else %>
  top.window.location = '<%= edit_video_url(@video) %>'
<% end %>
